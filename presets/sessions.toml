# PushOS: sessions
#
# The coding sessions you already have open, on pads.
#
# PushOS did not start these and does not own them: a pseudo-terminal belongs
# to whoever created it. What it can do is watch them and type into them, which
# is most of what a pad is for. It reads each window's title, which for a
# coding agent is what that session is working on, so the screen shows eight
# real names rather than eight device numbers.
#
# The first time you run this, macOS asks whether PushOS may control Terminal.
# Say yes; that permission is what makes all of this work, and `pushos doctor`
# reports on it.

[runtime]
home_page = "sessions"

[permissions]
# Typing into a live session runs whatever it makes of the keystrokes, which is
# the same thing a terminal action does and needs the same saying yes to.
granted = ["shell.execute", "media.control"]

[sessions]
# Nothing tells PushOS when a window opens, so it asks. Three seconds is faster
# than you open windows and slower than the display is redrawn.
watch = true
poll_ms = 3000

[[pages]]
id = "sessions"
name = "Sessions"
description = "What each window is working on, and what it last said"

[[pages]]
id = "answer"
name = "Answer"
description = "Reply to whichever session you are looking at"

# --- Moving about ------------------------------------------------------------

[[bindings]]
control = "button.upper_1"
gesture = "press"
action = "page.show"
target = "sessions"
label = "Sessions"

[[bindings]]
control = "button.upper_2"
gesture = "press"
action = "page.show"
target = "answer"
label = "Answer"

[[bindings]]
control = "button.session"
gesture = "press"
action = "page.home"
label = "Home"

# The two arrows beside the screen move between pages, wherever you are.
[[bindings]]
control = "button.page_right"
gesture = "press"
action = "page.next"
label = "Next page"

[[bindings]]
control = "button.page_left"
gesture = "press"
action = "page.previous"
label = "Previous page"

[[bindings]]
control = "button.play"
gesture = "press"
action = "media.play_pause"
label = "Play/Pause"

# --- Browsing without looking at the pads ------------------------------------
# The first encoder walks through the open sessions and opens each one as it
# arrives, so you can turn the knob and read. The up and down arrows do the
# same thing for a hand that is already on the buttons. Neither needs you to
# have chosen anything first: turning from nothing starts at one end.

[[bindings]]
control = "encoder.1"
gesture = "turn_right"
action = "session.next"
label = "Next session"

[[bindings]]
control = "encoder.1"
gesture = "turn_left"
action = "session.previous"
label = "Previous session"

[[bindings]]
control = "button.down"
gesture = "press"
action = "session.next"
label = "Next session"

[[bindings]]
control = "button.up"
gesture = "press"
action = "session.previous"
label = "Previous session"

# The second encoder moves back and forward through what the session you are
# reading has said. The display says how far back you are.

[[bindings]]
control = "encoder.2"
gesture = "turn_left"
action = "session.scroll"
label = "Back"
params = { by = 1 }

[[bindings]]
control = "encoder.2"
gesture = "turn_right"
action = "session.scroll"
label = "Forward"
params = { by = -1 }

# The touch strip does the same, for a thumb rather than a hand.
[[bindings]]
control = "button.left"
gesture = "press"
action = "session.scroll"
label = "Back"
params = { by = 2 }

[[bindings]]
control = "button.right"
gesture = "press"
action = "session.scroll"
label = "Forward"
params = { by = -2 }

# --- One pad per window ------------------------------------------------------
# Eight pads on the bottom row, one per terminal device. A tap selects that
# session and puts the last few lines of it on the screen; a hold brings its
# window to the front.
#
# `tty:ttys000` is exact and stable for as long as that window is open. If you
# would rather name them by what they are working on, write the words instead:
#
#   target = "Sprint 2"
#
# which matches any session whose title contains them, and keeps working when
# the window moves to another device.
#
# A session opened this way stays on the screen until you look away. Tapping
# another pad shows that one instead, and the button below returns you to the
# eight.

[[bindings]]
control = "button.upper_8"
gesture = "press"
page = "sessions"
action = "page.home"
label = "All eight"

[[bindings]]
control = "pad.56"
gesture = "tap"
page = "sessions"
action = "session.show"
target = "tty:ttys000"
label = "ttys000"

[[bindings]]
control = "pad.56"
gesture = "hold"
page = "sessions"
action = "session.focus"
target = "tty:ttys000"
label = "Show window"

[[bindings]]
control = "pad.57"
gesture = "tap"
page = "sessions"
action = "session.show"
target = "tty:ttys001"
label = "ttys001"

[[bindings]]
control = "pad.57"
gesture = "hold"
page = "sessions"
action = "session.focus"
target = "tty:ttys001"
label = "Show window"

[[bindings]]
control = "pad.58"
gesture = "tap"
page = "sessions"
action = "session.show"
target = "tty:ttys002"
label = "ttys002"

[[bindings]]
control = "pad.58"
gesture = "hold"
page = "sessions"
action = "session.focus"
target = "tty:ttys002"
label = "Show window"

[[bindings]]
control = "pad.59"
gesture = "tap"
page = "sessions"
action = "session.show"
target = "tty:ttys003"
label = "ttys003"

[[bindings]]
control = "pad.59"
gesture = "hold"
page = "sessions"
action = "session.focus"
target = "tty:ttys003"
label = "Show window"

[[bindings]]
control = "pad.60"
gesture = "tap"
page = "sessions"
action = "session.show"
target = "tty:ttys004"
label = "ttys004"

[[bindings]]
control = "pad.60"
gesture = "hold"
page = "sessions"
action = "session.focus"
target = "tty:ttys004"
label = "Show window"

[[bindings]]
control = "pad.61"
gesture = "tap"
page = "sessions"
action = "session.show"
target = "tty:ttys005"
label = "ttys005"

[[bindings]]
control = "pad.61"
gesture = "hold"
page = "sessions"
action = "session.focus"
target = "tty:ttys005"
label = "Show window"

[[bindings]]
control = "pad.62"
gesture = "tap"
page = "sessions"
action = "session.show"
target = "tty:ttys006"
label = "ttys006"

[[bindings]]
control = "pad.62"
gesture = "hold"
page = "sessions"
action = "session.focus"
target = "tty:ttys006"
label = "Show window"

[[bindings]]
control = "pad.63"
gesture = "tap"
page = "sessions"
action = "session.show"
target = "tty:ttys007"
label = "ttys007"

[[bindings]]
control = "pad.63"
gesture = "hold"
page = "sessions"
action = "session.focus"
target = "tty:ttys007"
label = "Show window"

# --- Answering ---------------------------------------------------------------
# Every one of these acts on whichever session you last selected, so one pad
# serves all eight.
#
# There are two ways to reply, and they are not the same thing. `session.send`
# types a line and submits it, which is how you answer a question. `session.press`
# presses a single key and submits nothing, which is how you accept what is
# being suggested: Tab takes the suggestion and leaves it there for you to look
# at, and Enter afterwards sends it.

[[bindings]]
control = "pad.0"
gesture = "tap"
page = "answer"
action = "session.send"
label = "Yes"
params = { text = "y\n" }

[[bindings]]
control = "pad.1"
gesture = "tap"
page = "answer"
action = "session.send"
label = "No"
params = { text = "n\n" }

[[bindings]]
control = "pad.2"
gesture = "tap"
page = "answer"
action = "session.send"
label = "Enter"
params = { text = "\n" }

[[bindings]]
control = "pad.3"
gesture = "tap"
page = "answer"
action = "session.send"
label = "Continue"
params = { text = "continue\n" }

# Accepting a suggested follow-up. Tab takes it, and the pad beside it sends
# it, so you can read what you are about to say before you say it.
[[bindings]]
control = "pad.4"
gesture = "tap"
page = "answer"
action = "session.press"
label = "Accept"
params = { key = "tab" }

[[bindings]]
control = "pad.5"
gesture = "tap"
page = "answer"
action = "session.press"
label = "Send it"
params = { key = "enter" }

# Moving through a list of choices, for when a session is offering several.
[[bindings]]
control = "pad.9"
gesture = "tap"
page = "answer"
action = "session.press"
label = "Up"
params = { key = "up" }

[[bindings]]
control = "pad.10"
gesture = "tap"
page = "answer"
action = "session.press"
label = "Down"
params = { key = "down" }

[[bindings]]
control = "pad.11"
gesture = "tap"
page = "answer"
action = "session.press"
label = "Dismiss"
params = { key = "escape" }

# Interrupting sends the control character, not the word, so a session waiting
# at a prompt is stopped rather than told about stopping. On a hold, because
# stopping something by brushing a pad would be worse than stopping it slowly.
[[bindings]]
control = "pad.7"
gesture = "hold"
page = "answer"
action = "session.interrupt"
label = "Interrupt"

[[bindings]]
control = "pad.8"
gesture = "tap"
page = "answer"
action = "session.show"
label = "What it said"

[[bindings]]
control = "button.upper_8"
gesture = "press"
page = "answer"
action = "page.back"
label = "Back"
